BRANCH=`git rev-parse --abbrev-ref HEAD`
PUSH_COMMAND=`ps -ocommand= -p $PPID`

if [[ "$BRANCH" =~ ^(prod|stage|dev)$ ]]; then

  echo "Pushing to branch prod stage or dev  is forbidden"

  exit 1
fi

exit 0